# Test PLN file with secret patterns
interpreter=/bin/sh

test_host:
	echo "Database password: {{secret:db_password}}"
	echo "API key: {{secret:api_key}}"
	echo "Regular text without secrets"
	echo "Mixed: user={{secret:username}} pass={{secret:password}}"